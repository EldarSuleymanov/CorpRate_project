import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    starsContainer: {
        flexDirection: 'row',
        flex: 0.1,
        marginHorizontal:50
    },
    starTouch: {
        flex: 1,
    },
});